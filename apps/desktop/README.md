# desktop

Desktop-Anwendung auf Basis von Tauri.

Inhalt:
- Web-Frontend (`app.js`, `index.html`, `styles.css`)
- Tauri-Backend (`src-tauri/`)
- Build-/Start-Skripte (`package.json`, `scripts/`)

Typische Befehle:
- `npm install`
- `npm run prepare:dist`
- `npx tauri dev`
